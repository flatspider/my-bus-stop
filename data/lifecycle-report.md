# GTFS-rt Trip Lifecycle Report

## Summary

| Metric | Value |
|--------|-------|
| Time range | 2026-03-03T14:39:22.345Z → 2026-03-03T16:19:02.639Z |
| Snapshots analyzed | 101 |
| Total unique trips tracked | 49 |
| **Ghost trips** | **0** (0 fallback, 0 real-time) |
| **Ghost rate** | **0.0%** of trips approaching this stop |
| Late pickups (SIRI eventually caught up) | 0 |
| SIRI throughout | 49 |
| Too far to confirm | 0 |
| Insufficient data | 0 |

> **Ghost rate** = ghost trips / (ghost + late_pickup + siri_throughout). This is the percentage of GTFS-rt trips that appeared to approach this stop but were never confirmed by SIRI.

## Route Breakdown

| Route | Approaching | Ghosts | Fallback | Real-time | Late Pickup | SIRI | Ghost Rate |
|-------|-------------|--------|----------|-----------|-------------|------|------------|
| M103 | 13 | 0 | 0 | 0 | 0 | 13 | 0% |
| M101 | 25 | 0 | 0 | 0 | 0 | 25 | 0% |
| M102 | 11 | 0 | 0 | 0 | 0 | 11 | 0% |

## Ghost Trip Cases

No ghost trips detected in this dataset.

## Late Pickup Cases

No late pickup cases — all SIRI-confirmed trips were visible from the start.

## Classification Counts

| Classification | Count | Description |
|---------------|-------|-------------|
| ghost (fallback) | 0 | GTFS-rt serving schedule data for a non-existent bus. `isFallback: true`. |
| ghost (real-time) | 0 | Has real-time data but SIRI never showed it. Rarer, more alarming. |
| late_pickup | 0 | Started GTFS-only, SIRI eventually caught up. Bus was real. |
| siri_throughout | 49 | SIRI had it from the start. Any GTFS-only is post-arrival noise. |
| too_far | 0 | ETA never dropped below 30min AND no VP within 500m. Can't confirm ghost. |
| insufficient_data | 0 | < 3 observations or < 2 min span. |

## Methodology

### What is a ghost bus?

A bus that GTFS-rt reports as approaching a stop (visible in Apple Maps) but SIRI never confirms (not visible in MTA BusTime). **SIRI is ground truth** — if SIRI doesn't show it, the bus doesn't exist for riders using BusTime.

### How this analysis works

1. **Index by tripId** across all snapshots. For each snapshot, scan both `gtfsOnlyTrips` (GTFS-rt only) and `vehicles` (SIRI-matched with tripId). Build a timeline per trip.

2. **Build a lifecycle** per tripId tracking: ETA progression, count of GTFS-only vs SIRI-matched observations, whether SIRI ever acknowledged it, `isFallback` status, and VP distance to stop.

3. **Classify** each lifecycle using a waterfall:
   - **insufficient_data** — < 3 observations or < 2 min span
   - **siri_throughout** — first observation was SIRI-matched, SIRI had it from the start
   - **late_pickup** — started GTFS-only, but SIRI eventually picked it up
   - **ghost** — SIRI never acknowledged AND (ETA got within 15min OR VP within 500m)
     - **ghost (fallback)** — `isFallback: true`, scheduled data for a non-existent bus
     - **ghost (real-time)** — real-time data but SIRI still never showed it
   - **too_far** — ETA never dropped below 30min AND no VP within 500m

### Key thresholds

| Parameter | Value | Meaning |
|-----------|-------|---------|
| ETA close | 15 min | Trip must have ETA under this to qualify as ghost |
| ETA far | 30 min | If ETA never dropped below this, classified as too_far |
| VP close | 500m | VP within this distance qualifies as approaching |
| Min observations | 3 | Minimum snapshots to analyze a trip |
| Min span | 2 min | Minimum time span to analyze a trip |

---

*Generated by BusWatch trip lifecycle analysis — 2026-03-03T16:37:53.790Z*
